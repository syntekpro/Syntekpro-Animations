# Presets Library

Preset definitions for blocks and shortcodes. JSON-driven for export/import.
- `json/` holds preset bundles (hero/card/cta/stats/etc.).

TODO (2.0):
- Add schema for presets (type, trigger, duration, ease, engine preference).
- Build import/export UI in admin.
