# CSS Animations (Light Mode)

Home for CSS keyframes and mapping tables to support the light-mode engine.

TODO (2.0):
- Split keyframes into modular files if needed.
- Add mapping doc for preset → class → keyframe.
