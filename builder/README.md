# Builder (Timeline UI)

Stubs for the upcoming visual timeline/tweener editor.
- `js/` for timeline logic (playhead, keyframes, scrubbing).
- `css/` for UI skin.

TODO (2.0):
- Initialize React/vanilla view, play/pause, scrub, easing curve picker.
- Grouped sequences and keyframe list.
